Hello <%= @user.person.full_name %>,

Enter this code in the browser where you requested the email:

<%= @login_code %>

Or use this single-use link to sign in to LegionPostTools within 15 minutes:

<%= @login_url %>

The code and link each work once. Using either one invalidates the other.

There's no password to remember. If you didn't request this, you can ignore this email.

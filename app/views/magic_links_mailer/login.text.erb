Hello <%= @user.person.full_name %>,

Enter this code in the browser where you requested the email:

<%= @login_code %>

Or use this single-use link to continue in LegionPostTools within 15 minutes. If you
are already signed in and confirming a change, open it in that same browser:

<%= @login_url %>

The code and link each work once. Using either one invalidates the other.

If you didn't request this, you can ignore this email.
